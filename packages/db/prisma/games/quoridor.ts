export const quoridorCatalog = {
  name: "路墙棋",
  summary: "移动棋子或设置两格长墙，率先抵达对边即可获胜。",
  description:
    "双方各控制一枚棋子和十面墙，通过移动、跳跃与布墙争夺通往对边的路线。平台会编译你的 C++ 程序，并让它分别挑战本游戏下的全部启用内置 AI。",
  rulesMarkdown: `## 基本规则

棋盘由 $9 \\times 9$ 个棋格组成，坐标从 0 开始。左上角为 $(0,0)$，$X$ 向右增加，$Y$ 向下增加，范围均为 0 到 8。

座位 0 先手，棋子从 $(4,0)$ 出发，率先抵达任意 $Y=8$ 的棋格即获胜；座位 1 后手，棋子从 $(4,8)$ 出发，率先抵达任意 $Y=0$ 的棋格即获胜。棋子不需要到达对面的起点，抵达目标边上的任意棋格即可。

每方初始拥有十面墙。每回合必须且只能选择一种行动：移动自己的棋子，或者消耗一面墙并将其放到棋格之间。墙一旦放下便不能移动或收回。

### 棋子移动

棋子通常可以向上、下、左、右移动一个棋格，但不能越出棋盘、穿过墙或移动到对手所在的棋格。棋子可以横向或向后移动，不要求每一步都接近目标边。

如果对手位于没有墙阻挡的相邻格，并且对手身后的棋格仍在棋盘内、没有墙阻挡，则可以沿同一方向直线跳过对手。例如己方位于 $(4,4)$、对手位于 $(4,5)$ 时，可以跳到 $(4,6)$。

只有当对手身后的棋格越界或被墙阻挡时，才可以斜跳到对手两侧。例如上述位置中的 $(4,6)$ 无法进入时，可以尝试移动到 $(3,5)$ 或 $(5,5)$；对手与斜跳目的地之间也不能有墙。如果能够直线跳过对手，就不能改为斜跳。

### 放置墙

墙长为两个棋格边，使用四个相邻棋格之间的内部交叉点作为中心坐标 $(X,Y)$，因此墙坐标的范围均为 1 到 8。

方向 0 表示竖墙，方向 1 表示横墙。例如：

- 竖墙 $(4,2,0)$ 阻断 $(3,1)$ 与 $(4,1)$ 之间、以及 $(3,2)$ 与 $(4,2)$ 之间的移动；
- 横墙 $(4,2,1)$ 阻断 $(3,1)$ 与 $(3,2)$ 之间、以及 $(4,1)$ 与 $(4,2)$ 之间的移动。

新墙不能与已有墙重叠或交叉。只在端点相接且没有重叠的墙可以同时存在。放墙后，双方棋子都必须仍至少保留一条通往各自目标边的路径，不允许用墙彻底封死任意一方。

若双方各完成 100 步后仍无人抵达目标边，对局结束且用户程序判负。当前版本没有其他和棋终局。

## 程序通信协议

程序启动后首先读取两个整数：协议版本和己方座位。

~~~text
1 S
~~~

协议版本目前固定为 1，\`S\` 只能为 0 或 1。棋盘大小、初始位置和墙数由当前规则固定，不会在初始化中另行传递。

座位 0 读取初始化信息后立即输出第一步；座位 1 会先收到座位 0 的第一步，然后再输出自己的第一步。此后每次轮到己方时，先从标准输入读取对手刚完成的一步，更新本地局面，再输出己方的一步。

移动棋子时输出：

~~~text
0 X Y
~~~

其中 $(X,Y)$ 是移动后的目标棋格。

放墙时输出：

~~~text
1 X Y O
~~~

其中 $(X,Y)$ 是墙的中心坐标，\`O=0\` 表示竖墙，\`O=1\` 表示横墙。

每一步必须单独占一行，并在输出后立即刷新标准输出。评测器只接受对应格式所需的整数，不能输出额外字段或调试文字；调试信息应写入标准错误。输入流结束表示对局已经结束，评测器不会发送额外的结束标记。

## 交互样例

以下是座位 0 程序的一段通信。以 \`>>\` 开头的代表输入，\`<<\` 开头的代表输出。

~~~text
>> 1 0
<< 0 4 1
>> 0 4 7
<< 1 4 2 1
>> 0 3 7
<< 0 3 1
>> 1 5 7 0
<< 0 3 2
~~~`,
  rulesVersion: "quoridor-standard-v1",
  isPublished: true,
};
